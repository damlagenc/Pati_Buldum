import 'package:flutter/material.dart';

class SahiplenmePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.teal.shade400,
        title: Text("Sahiplen"),
      ),
      body: Center(
        child: Text("Sahiplen"),
      ),
    );
  }
}
